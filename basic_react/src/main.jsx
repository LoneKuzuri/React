import React from "react";
import { createRoot } from "react-dom/client"
import Footer from "./chiya.jsx";
import "./app.css";

const root = createRoot(document.getElementById("root"));

root.render(
 
    <Footer />
  
);
